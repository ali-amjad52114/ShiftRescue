node_modules/
.next/
.env
.env.local
npm-debug.log*
*.tsbuildinfo
.vercel
.env*.local
.aidesigner/*
!.aidesigner/.gitkeep
.env*

Call_list - Sheet1.csv
campaign-template.csv

# Local working notes. These contain demo phone numbers and live resource ids,
# so keep the rule here rather than in .git/info/exclude, which is never cloned.
ARCHITECTURE.md
SWAYAM-WORK.md
.playwright-mcp/
